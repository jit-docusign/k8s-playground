apiVersion: v1
kind: Secret
metadata:
  name: api-auth-secret
  namespace: microservices
  labels:
    app: microservices
type: Opaque
stringData:
  # API Key for service-to-service authentication
  # OrderApi uses this to authenticate with ProductApi
  API_KEY: product-api-secret-key-12345
