apiVersion: v1
kind: Service
metadata:
  name: product-api
  namespace: microservices
  labels:
    app: product-api
spec:
  type: ClusterIP
  selector:
    app: product-api
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  sessionAffinity: None
